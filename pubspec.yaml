name: currencyx
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0            # आपका http पैकेज
  dropdown_search: ^6.0.1 # आपका dropdown_search पैकेज

flutter:
  uses-material-design: true